# ALMS 本地站维护说明

人物内容来自 `archive-data.js`，城市与装潢来自 `page-media.js`。修改后刷新 `index.html` 即可；若浏览器仍显示旧内容，使用 Ctrl+F5。数据使用双引号，保留字段之间的逗号与文件末尾分号。这个说明仅供本地维护，不会显示在页面中。

## 填充预留档案

当前有六个空位：冰星防卫 `ice-03`、`ice-04`，北方机械制造厂 `ship-02`、`ship-03`，北德文斯克海军基地 `ru-02`、`ru-03`。`ship-01` 不是当前预留编号。找到目标记录后修改，例如：

```js
{
  "id": "ice-03",
  "faction": "ice",
  "name": "人物完整姓名",
  "en": "FULL NAME",
  "affiliation": "冰星防卫",
  "image": "assets/ice-03.webp",
  "detail": "assets/ice-03-large.webp",
  "description": "第一段介绍。\n\n第二段介绍。",
  "reserved": false,
  "gallery": [
    {
      "title": "供维护识别的名称",
      "thumb": "assets/ice-03-design-thumb.webp",
      "detail": "assets/ice-03-design-large.webp"
    }
  ]
}
```

`image` 是立绘预览，`detail` 是点击后加载的大图，留空会使用 `image`。透明立绘可用 WebP 或 PNG，预览最长边约 2000 像素，高清版本约 3000–4000 像素。`image` 留空时显示“立绘情报待公开”，不会显示破图。`reserved: true` 表示整份档案尚未公开，填好后改为 `false`。填充空位时保持原 `id`，名称使用全名。

## 新增人物与附图

复制 `people` 数组中的一条记录，换成唯一的 `id`。阵营值对应：`ice` 冰星防卫，`ship` 北方机械制造厂，`russian` 北德文斯克海军基地，`linked` 关联档案。数组顺序决定目录内排序。顶部目录和阅读时的“切换档案”目录都自动读取这些数据，无需维护两份名单。

每个人的 `gallery` 管理附图，数组顺序就是展示和大图翻页顺序。`title` 仅便于维护，页面不展示图片说明；`source` 可选，仅记录原始文件名。白芷的装备、空投、证卡横图等均在她自己的 `gallery`；训练设施在南希资料；感染体和“？？？”也在关联档案中各自管理。

图集以紧凑的固定高度图册展示，一次查看一张完整图片，支持左右翻页和下方缩略图跳转，手机可水平滑动。横图和竖图均完整缩放放入图框，不裁切，翻页不会改变图框高度。只有一张图时不显示翻页箭头和缩略图索引。页面使用缩略图和网页预览，打开查看器才加载高清文件。新图即使尚未生成尺寸索引也会完整放入固定图框；为生成清晰的网页预览，建议在加完图片后运行 `python tools/build_assets.py`。

脚本依赖 Pillow，读取站内高清文件，只写入 `assets/web` 和 `media-sizes.js`，不会改动原图。图库预览最长边 1400 像素，城市与装潢预览为 1600、2400 像素。生成后的文件需要与网页一起保留。通常只改文案或排序不需要运行这个脚本。

## 组织标识、服装与档案附页

阵营的 `logo` 是默认组织标识；人物 `logo` 可以覆盖它。南希沿用冰星军事火器集团标识，不要恢复已移除的组织关系栏。`backgroundLogo` 可单独指定档案底图；“？？？”的标识继续作为底图，不作为独立图库图片。

背景 LOGO 由 `archive-ui.js` 放入固定的独立背景层，不跟随正文或图集高度重新居中。新增资料时不要把标识重新放进文章或图库，以免切换时位置改变。

白芷的 `variants` 管理服装，每项包含 `label`、`image`、`detail`。诺里斯的 `identities` 管理附页，第二份记录可覆盖姓名、标识、立绘和介绍。公开目录一直使用基础记录的姓名和第一份公开标识；不要把历史身份写进公开姓名、目录、标签或新提示。继续沿用已确认的旧站介绍。

`sheetOnly: true` 仅用于以整页设定图为主体、没有独立立绘的资料；梅塔科夫已于 2026-09-19 更新为 `sheetOnly: false`，使用完整立绘与介绍，并保留原设定图册。`galleryOnly: true` 用于只展示图集的感染体和“？？？”。同一阵营的目录结构由整个阵营决定，未公开档案不另设一套排版。

## 城市图集和手机取景

`page-media.js` 的 `scenes` 控制城市图集。目前原图顺序为 `9.jpg`、`10.jpg`、`8.jpg`、`5.jpg`、`4.jpg`、`3.jpg`、`2.jpg`、`1.jpg`。`mobilePosition` 是手机竖屏裁切焦点，例如 `"68% 50%"`，前一个数字向右增大便会更偏向图片右侧；只改变网页取景，不会裁剪原文件。

`decor` 保存两张 CONCEPT 装潢图。新增城市图后运行预览脚本。如果更换第一张城市图，也把 `index.html` 中 `.region-bg` 的初始 `src`、`srcset` 和 `alt` 同步为新图，避免脚本启动前闪过旧首图。

## 修改样式和交互

首页只加载根目录的 `site.css`，它由以下可维护模块合并生成。不要直接编辑生成结果，否则下次合并会覆盖修改。

| 文件 | 内容 |
| --- | --- |
| `styles/00-base.css` | 全局基础、字体与通用规则 |
| `styles/10-hero.css` | 首页静态底图、文字及响应式布局 |
| `styles/20-world.css` | 世界、城市、尾页 |
| `styles/40-archives.css` | 阵营、人物、立绘与固定背景标识 |
| `styles/50-gallery.css` | 资料入口、紧凑图册和缩略图索引 |
| `styles/60-viewer.css` | 大图查看器 |
| `styles/65-navigation.css` | 阅读浮栏与档案切换目录 |
| `styles/70-graphics.css` | 平面装饰 |

每个模块包含自己的手机和减少动态效果规则。修改后在站点根目录运行 `python tools/build_styles.py`，无需安装 npm。模块内资源路径相对于根目录生成的 `site.css`，例如 `assets/fonts/...`。

交互分工：`archive-ui.js` 展示人物；`media-ui.js` 展示附图和城市轮播；`album-ui.js` 控制图册翻页、缩略图索引、手势和图片加载；`viewer.js` 控制大图翻页、缩放和返回阅读位置；`archive-navigation.js` 控制阅读浮栏和切换目录。首页已于 2026-09-22 改为静态底图，原 hero-motion.js 和相关特效层已移除；城市背景的轻微鼠标随动继续由 polish.js 管理。`polish.js`、`signal.js`、`refinement.js` 保留现有页面细节交互。

旧版独立 CSS 和旧版 app.js、intel.js、library-data.js、library-ui.js 已清理。日常修改请使用上述当前文件。

## 本地预览与备份

双击 `index.html` 可直接查看，或运行“启动预览.cmd”。后者调用本机 `C:\Python314\python.exe`，窗口关闭后预览会停止；换电脑时按实际 Python 路径调整。预览仅绑定本机地址，不会自动发布到互联网。

更新前的可用版本已集中为站点文件夹外的一份 ZIP，路径见“本次视觉调整与回退.md”。旧备份目录与未引用导出已移入回收站。以后请将回退文件放在站点之外，不要把历次原画或网页副本一起上传。

## 上传前检查

运行 `python tools/check_site.py` 检查缺失资源、闲置素材和单文件 25 MiB 限制。脚本只报告，不删除文件；“闲置素材”需要结合当前代码确认后处理。每次换图后重新生成 media-sizes.js，并同步 index.html 中的数据脚本版本号。当前梅塔为 metakov-r4 的两张 WebP，后续更新时只在站点保留当前版本。

GitHub 网页每批最多 100 个文件，因此整站需要分批上传，或使用 GitHub Desktop。具体步骤见 README.md。`.gitignore` 仅在通过 Git 提交时起作用，网页拖拽时仍需自行避开原画、备份和压缩包。
