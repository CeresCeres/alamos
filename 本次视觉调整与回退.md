## 2026-09-23 章节顺序调整

人物档案页与“北德文斯克”页调换位置。`.region` 原本嵌在 `#story` 内、紧跟“世界”之后；现已移出为 `main` 下的独立区块，排在 `#characters` 之后、`#transmission` 之前。页面顺序变为：首页 → 世界 → 人物 → 北德文斯克 → 更多情报。

仅改动 `index.html` 的结构位置，类名、样式与脚本均未改动：`polish.js`、`media-ui.js` 与 `signal.js` 都按类名取元素，不受元素在文档中的先后影响；两页各自的翻页框、鼠标随动与 Kicker 编号（01 / WORLD、02 / PERSONNEL ARCHIVES）保持原样，未重排数字。`check_site.py` 仍无缺失引用与闲置素材。

---

## 2026-09-22 鼠标随动扩展与底部浮窗移除

第三页的鼠标随动推广到第二页（世界）与尾页（更多情报）：polish.js 的 section 集合加入 `.story-opening` 与 `.transmission`，两张底图的 CSS 改为与第三页相同的 `translate3d(var(--drift-x),var(--drift-y),0)` 加各自缩放；第二页幅度与第三页一致，尾页因图层更淡取较小幅度。仍遵守减少动态效果设置，且仅在桌面精确指针下生效。

人物档案页下滑后出现的底部“正在查阅”浮窗（`#archive-dock`）整体删除：移除 HTML 结构、65-navigation.css 中的相关规则与 archive-navigation.js 里的显示/隐藏逻辑。阵营与人物仍可通过页面上方的目录与人物标签切换；原浮窗里的“切换档案”弹窗代码保留但暂无入口。已同步更新 index.html、CSS 与脚本缓存版本。

核对：`check_site.py` 无缺失引用、无闲置素材，仍为 176 个文件、43.66 MiB；脚本语法检查通过。本轮改动已随后推送到远程仓库并在线验证。

---

## 2026-09-22 第二页图册与尾页主视觉

第二页（世界）参照第三页“北德文斯克”的做法，改为可翻页底图图册：右下角复用同一套翻页框与进度条，支持按钮、方向键和左右滑动。图册收录第二页原有“远望城市”（concept1）与原尾页“设施深处”（concept2），顺序与数据文件中的 world 组一致；移动端按每张图各自的取景位置显示。

尾页（更多情报）不再使用“设施深处”，改为主视觉：`E:\pitrues\Key Vision.png`（6000×3360）导出 `assets/key-vision-large.webp`（3200px，WebP 质量 84，约 0.45 MiB），并按 hero 的三档方式生成 960/1600/2400 预览（58/131/241 KiB）。原 PNG 不进站点，也不改动。

脚本改动：`media-ui.js` 把原先只服务第三页的翻页逻辑抽成 `createSceneScroller`，第三页与第二页共用；`polish.js` 的进度条改为按各翻页框自行更新。`build_assets.py` 的背景预览补上 960 档并包含 world 组。已更新 index.html、CSS 与相关脚本的缓存版本。

核对：`check_site.py` 无缺失引用、无闲置素材，最大单文件仍为 1.81 MiB；176 个文件、约 43.66 MiB。本地 HTTP 预览中首页、样式、脚本与新主视觉均正常返回。此次未创建 Git 提交或上传远程。

---

# 2026-09-22 首页静态底图更新

站点当前位置：G:\ALMS页。新原图实际路径：E:\pitrues\ALMS\Key Vision2.png（6000×3360，2026-09-22 17:18 更新）。压缩为 960、1600、2400px 三档 WebP，约 94、280、820 KiB。原画不改动，不把 50 MiB 的原始 PNG 放进待上传站点。

修改前完整站点、旧首页原始 PNG、动效脚本、所有文字与字体装饰已保存于 `G:\ALMS回退\首页静态替换前-20260922-201625.zip`。备份在站点目录之外，ZIP 已验证完整性。回退时将其中 site 解压至新目录，打开 index.html 即可；原始首页底图在单独目录中。

删除当前站点的 hero-motion.js 与旧首页三档 WebP，移除相关 canvas、镜头、光学及人物动作样式；关闭首页鼠标随动。网页标题、蓝色 Z、按钮、边框、斜杠、渐隐遮罩和底部文字保留。新图右下角自带标题按原图保留。手机取景调整到 26% 以露出左侧人物。其他章节与人物资料数据不变。

验证：桌面和 390×844 手机预览加载正常，首页无动效画布、无镜头层、背景 transform 为 none，手机无横向溢出；资源检查没有缺失或闲置资产，最大单文件仍为 1.81 MiB。整站约 42.46 MiB，161 个文件。GitHub 网页上传仍应分成每批不超过 100 个文件，方法见 README.md。

---

# 最近一次更新与回退

2026-09-19：同步 ALMS 中修改时间为 18:30:48 的“梅塔.png”。导出 metakov-r4-2000.webp（1156×2000）与 metakov-r4-detail.webp（2082×3600），更新人物图与点击放大的高清图。保留透明背景、原有排版、淡色渐变、人物文案、原设定图册和首页动效。原始 PNG/PSD 没有改动。

## 文件精简

清理前约 61.34 MiB、527 个文件。已将站内旧备份目录、旧版 CSS/JS 和 63 个不再引用的素材移至回收站；保留正在使用的所有素材、响应式尺寸、字体和维护工具。旧的分次修改记录不再重复保存在站点目录中。

## 一份可回退快照

更新前可用版本：`G:\ALMS回退\ALMS更新精简前-20260919-184513.zip`。

该 ZIP 位于站点文件夹之外，包含上一版网站运行所需资源、样式源模块、工具及维护文档，已通过 ZIP 完整性检查；没有再收集历次备份。需要回退时，将 ZIP 解压到一个新的独立文件夹，打开其中 index.html，或将启动脚本的预览目录改为解压目录。不要把 ZIP 上传进站点仓库。

## 上传与检查

运行 `python tools/check_site.py` 查看当前实际大小、缺失资源和单文件限制。GitHub 网页上传需要按每批不超过 100 文件拆分，操作方法见 README.md。此次没有创建 Git 提交、重写 Git 历史或上传至远程服务。

验证结果：最新梅塔展示图及 3600px 高清图加载正常，原设定图册与城市轮播可用，浏览器未出现错误日志。清理后的引用检查无缺失、无闲置素材；共 162 文件、约 41.83 MiB，最大单文件约 1.81 MiB。页面样式和交互脚本与更新前快照逐字节一致。

## 2026-09-22 首页构图与鼠标随动

移除首页晕影遮罩和网页叠加的 ALAMOS / ZWEI 标题，仅保留底图自带标题。原标题上方色块独立放到左上角，与右上装饰色块中心对齐。桌面底图底边上移约 84px，为原图 ZWEI 与底部信息留出空间；手机竖屏完整显示横图，避免裁掉原图字样。恢复桌面细微鼠标随动，尊重减少动态效果设置；天气、人物动作及镜头自动晃动仍不加载。

本次修改前代码备份：G:\ALMS回退\首页构图调整前-20260922-204809.zip（覆盖其中同名文件可回退本轮布局）。完整原首页备份继续保留于同目录。已核对桌面与手机预览、原图字样完整性，以及实际鼠标操作后背景位移值发生变化。

## 2026-09-22 首页满幅与反向飘雪

首页按原画 25:14 比例决定高度，取消固定屏高与底部预留黑区；电脑、手机都显示完整横图。“浏览情报”与上方小字一起位于左侧垂直居中。桌面保留极轻微鼠标随动，以同图底层覆盖移动露出的边缘，原画未重压缩。

从旧版恢复独立飘雪层 hero-snow.js，保持已确认的 60% 密度与 70% 速度，两轴方向同时反转，改为向右上飘动；不加载人物动作、火花和自动镜头。页面隐藏、首屏离开视口或系统要求减少动态效果时暂停。

已验证桌面与 390×844 手机预览：首页按图像比例铺满，无下部黑框、无横向溢出，原图标题可见，按钮组合垂直居中，雪花画布正常绘制，浏览器无错误。修改前代码备份：G:\ALMS回退\首页满幅与反向雪前-20260922-211814.zip。


## 2026-09-22 底图来源与雪向修正
使用 E:\pitrues\Key Vision2.png（21:26:36 更新），维持 960/1600/2400px 与 WebP 质量 84/86/88，最大导出约 612 KiB。飘雪改为左上向右下，修正垂直位移、边界重生和雪痕倾角，密度与速度不变。已更新图片、CSS 和脚本缓存版本并确认浏览器加载新图。备份：G:\ALMS回退\首页底图与雪向修正前-20260922-214222.zip。
